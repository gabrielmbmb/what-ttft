version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - bodyclose
    - durationcheck
    - errcheck
    - errorlint
    - godoclint
    - gosec
    - govet
    - ineffassign
    - nilerr
    - noctx
    - nolintlint
    - revive
    - staticcheck
    - unconvert
    - unused

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true

    godoclint:
      default: basic
      enable:
        - require-doc
        - require-pkg-doc
      options:
        require-doc:
          ignore-exported: false
          ignore-unexported: true

    gosec:
      excludes:
        - G117 # Allow documented config structs to expose API-key fields without logging them.
        - G704 # Avoid noisy SSRF taint false positives for caller-configured benchmark/test URLs.
        - G705 # Avoid noisy taint false positives for internal printf helpers with controlled call sites.

    govet:
      enable:
        - shadow

formatters:
  enable:
    - gofumpt
    - goimports
